# ═══════════════════════════════════════════════════════════════
# Backend Environment Configuration
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────
# REQUIRED VARIABLES (Server will not start without these)
# ─────────────────────────────────────────────────────────────────

# Telegram Bot Token (from @BotFather)
# Required for: Telegram SSO authentication
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Telegram Admin Chat ID (for admin notifications)
# Get your chat ID by messaging @userinfobot on Telegram
# Can be a user ID, group ID, or channel ID (prefix with -100 for channels)
TELEGRAM_ADMIN_CHAT_ID=your-admin-chat-id

# JWT Secret (generate a secure random string, min 32 chars)
# Required for: Session token signing and verification
JWT_SECRET=your-jwt-secret-key-min-32-characters

# Supabase URL (from Supabase project settings)
# Required for: Database connection
SUPABASE_URL=https://your-project.supabase.co

# Supabase Service Key (from Supabase project settings > API)
# Required for: Server-side database operations with admin privileges
SUPABASE_SERVICE_KEY=your-supabase-service-key

# ─────────────────────────────────────────────────────────────────
# OPTIONAL VARIABLES
# ─────────────────────────────────────────────────────────────────

# Server Configuration
PORT=5001
NODE_ENV=development
DEBUG=false

# Supabase Anon Key (for client-side operations if needed)
SUPABASE_ANON_KEY=your-supabase-anon-key

# Browser Configuration
BROWSER_HEADLESS=true

# Auth Site Credentials (for WooCommerce/Yogateket login flows)
AUTH2_EMAIL=your-email@example.com
AUTH2_PASSWORD=your-password
AUTH3_EMAIL=your-email@example.com
AUTH3_PASSWORD=your-password

# Global Proxy Configuration - DEPRECATED
# Per-gateway proxy configuration is now managed through the Admin Panel
# See: Admin Dashboard > Gateway Management > Proxy Settings
# PROXY_URL=myuser:mypass123@138.68.11.9:8080
# PROXY_ENABLED=false
